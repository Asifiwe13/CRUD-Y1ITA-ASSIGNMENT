This is the implemented complete CRUD(CREATE, READ, 
UPDATE, DELETE) system in PHP connected to a MySQL database.


the project i have chosed is tool_library which shows the tool items, category, description, and condition it is,
this allows the follow up for efficiency and management in its daily use 
